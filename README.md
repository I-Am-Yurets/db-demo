# 🎖️ Military Aid Database - Spring Boot Project

**Лабораторна робота №3** з дисципліни "Технології прикладного програмування"  
Система обліку військової допомоги з використанням **Spring Boot + Spring Data JPA + PostgreSQL**

---

## 📋 Опис проекту

Веб-додаток для керування даними про військову допомогу з повним функціоналом **CRUD** (Create, Read, Update, Delete).  
Реалізовано 3-рівневу ієрархічну структуру бази даних з каскадними операціями.

### 🎯 Основні можливості

✅ **Додавання**, **редагування**, **видалення** та **перегляд** записів  
✅ **Пошук** по всіх таблицях  
✅ **Валідація даних** на Backend та Frontend  
✅ **Захист від SQL Injection** через Spring Data JPA  
✅ **Каскадне видалення** пов'язаних записів  
✅ Окрема сторінка для **табличного перегляду** даних

---

## 🗄️ Структура бази даних

### Ієрархія сутностей (3 рівні)

```
🌍 Country (Країна-донор)
   └── 📅 Period (Період допомоги)
         └── 🔫 Weapon (Передана зброя)
```

### Таблиці та зв'язки

#### 1️⃣ **countries** (Країни-донори)
| Поле | Тип | Опис |
|------|-----|------|
| `id` | BIGINT | PRIMARY KEY |
| `name` | VARCHAR(100) | Назва країни (UNIQUE) |
| `total_aid_usd` | NUMERIC | Загальна допомога (USD) |

**Зв'язок:** `1 Country → Many Periods`

---

#### 2️⃣ **periods** (Періоди допомоги)
| Поле | Тип | Опис |
|------|-----|------|
| `id` | BIGINT | PRIMARY KEY |
| `country_id` | BIGINT | FOREIGN KEY → countries |
| `period_name` | VARCHAR(100) | Назва періоду |
| `start_date` | DATE | Дата початку |
| `end_date` | DATE | Дата закінчення (nullable) |
| `aid_amount_usd` | NUMERIC | Сума допомоги (USD) |

**Зв'язок:** `Many Periods → 1 Country` + `1 Period → Many Weapons`

---

#### 3️⃣ **weapons** (Передана зброя)
| Поле | Тип | Опис |
|------|-----|------|
| `id` | BIGINT | PRIMARY KEY |
| `period_id` | BIGINT | FOREIGN KEY → periods |
| `weapon_type` | VARCHAR(100) | Тип зброї |
| `weapon_name` | VARCHAR(200) | Назва зброї |
| `quantity` | INTEGER | Кількість одиниць |
| `unit_cost_usd` | NUMERIC | Вартість одиниці (USD) |
| `total_cost_usd` | NUMERIC | Загальна вартість (USD) |

**Зв'язок:** `Many Weapons → 1 Period`

---

## 🏗️ Архітектура (3-tier)

```
📱 Presentation Layer (Controller)
   ↓
💼 Business Logic Layer (Service)
   ↓
🗄️ Data Access Layer (Repository)
   ↓
🐘 PostgreSQL Database
```

### Компоненти системи

| Шар | Технологія | Призначення |
|-----|-----------|-------------|
| **View** | Thymeleaf + HTML/CSS | Відображення UI |
| **Controller** | Spring MVC | Обробка HTTP запитів |
| **Service** | Spring Service | Бізнес-логіка + валідація |
| **Repository** | Spring Data JPA | Доступ до БД |
| **Database** | PostgreSQL 17.2 | Збереження даних |

---

## 🛡️ Безпека та валідація

### Backend валідація (Service Layer)
✅ Перевірка на `null` та порожні рядки  
✅ Мінімальна/максимальна довжина полів  
✅ Перевірка діапазонів числових значень  
✅ Валідація логіки дат (`startDate ≤ endDate`)  
✅ Перевірка існування зовнішніх ключів

### Frontend валідація (HTML5)
✅ Атрибут `required` для обов'язкових полів  
✅ `type="number"` з `min="0"` для сум  
✅ `type="date"` для дат  
✅ Нативні браузерні повідомлення

### Автоматичний захист
✅ **SQL Injection** — захищено через JPA Prepared Statements  
✅ **XSS** — захищено через автоекранування Thymeleaf

---

## 🚀 Запуск проекту

### Вимоги
- **Java 17** або новіше
- **Maven 3.9+**
- **Docker** (для PostgreSQL)

### Крок 1: Запуск бази даних
```bash
# Піднімаємо PostgreSQL + pgAdmin через Docker Compose
docker-compose up -d

# Перевірка статусу
docker ps
```

**Доступ до БД:**
- **PostgreSQL:** `localhost:5555`
- **pgAdmin:** [http://localhost:5050](http://localhost:5050)
    - Email: `admin@local.com`
    - Password: `admin`
- **Connection вкладка:**
    - Host: postgres (або localhost)
    - Port: 5432 
    - Maintenance database: military_aid_db 
    - Username: postgres 
    - Password: 12345

### Крок 2: Запуск Spring Boot додатку
```bash
# Збірка проекту
./mvnw clean install

# Запуск
./mvnw spring-boot:run
```

### Крок 3: Відкрити у браузері
🌐 **Головна сторінка (CRUD):** [http://localhost:8080](http://localhost:8080)  
📊 **Перегляд таблиць:** [http://localhost:8080/viewAll](http://localhost:8080/viewAll)

---

## 📱 Функціонал інтерфейсу

### Головна сторінка (`/`)
- ➕ **Додавання** нових записів через форми
- 🔍 **Пошук** записів за назвою/типом
- ✏️ **Редагування** існуючих записів
- 🗑️ **Видалення** записів (з каскадним видаленням)

### Сторінка перегляду (`/viewAll`)
- 📊 **Табличний формат** всіх даних
- 👁️ Режим **тільки для читання**
- ✏️ Швидкий доступ до редагування



---

## 🧪 Тестові дані

При першому запуску автоматично завантажуються тестові дані:

**3 країни:**
- USA ($75 млрд)
- Germany ($28 млрд)
- United Kingdom ($15 млрд)

**3 періоди:**
- 2022 Q1-Q2 (USA)
- 2023 Full Year (USA)
- 2022-2023 (Germany)

**3 види зброї:**
- M777 Howitzer (90 шт.)
- Javelin Anti-Tank Missile (5,000 шт.)
- Leopard 2 Tank (18 шт.)

---



## 🎓 Технічний стек

| Технологія | Версія | Призначення |
|-----------|--------|-------------|
| **Java** | 17 | Мова програмування |
| **Spring Boot** | 2.7.18 | Фреймворк |
| **Spring Data JPA** | 2.7.18 | ORM/Hibernate |
| **PostgreSQL** | 17.2 | База даних |
| **Thymeleaf** | 3.0.15 | Шаблонізатор |
| **Maven** | 3.9.11 | Збірка проекту |
| **Docker** | Latest | Контейнеризація БД |

---

## 📝 Автор

**Проект виконано для лабораторної роботи №3**  
Дисципліна: *"Технології прикладного програмування"*

---

## 📄 Ліцензія

Навчальний проект. Використовується виключно для освітніх цілей.

---

## 🔗 Корисні посилання

- [Spring Boot Documentation](https://docs.spring.io/spring-boot/docs/current/reference/html/)
- [Spring Data JPA Guide](https://spring.io/guides/gs/accessing-data-jpa/)
- [Thymeleaf Documentation](https://www.thymeleaf.org/documentation.html)
- [PostgreSQL Docs](https://www.postgresql.org/docs/)

---

**📧 Підтримка:** Якщо виникли питання, створіть Issue в репозиторії.