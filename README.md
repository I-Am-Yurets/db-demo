# 🎖️ Система обліку військової допомоги

Веб-застосунок для управління базою даних військової допомоги з повноцінною системою запитів та розмежуванням прав доступу.

---

## 🚀 Що це?

Це Spring Boot проєкт, який дозволяє:
- 📊 Вести облік військової допомоги від різних країн
- 📋 Створювати та обробляти запити на зброю
- 🔐 Розмежовувати доступ (адміністратор і звичайний користувач)
- 🌐 Працювати через веб-інтерфейс або REST API

---

## 📦 Структура даних

```
🌍 Країни-донори
   └── 📅 Періоди допомоги
         └── 🔫 Зброя

🏢 Організації-донори
   └── 📦 Поставки зброї

📋 Запити на допомогу (нова фіча!)
   ├── Статуси: PENDING → APPROVED/REJECTED → DELIVERED
   ├── Пріоритети: URGENT, HIGH, MEDIUM, LOW
   └── Бізнес-правила: зачинені країни не приймають запити
```

---

## 🎮 Швидкий старт

### 1. Запустіть базу даних
```bash
docker-compose up -d
```

### 2. Застосуйте міграцію для запитів (якщо ще не зроблено)
```bash
psql -U postgres -d military_aid_db -f aid_requests_migration.sql
```

### 3. Запустіть проєкт
```bash
./mvnw spring-boot:run
```

### 4. Відкрийте у браузері
- **Веб-інтерфейс:** http://localhost:8080
- **Перегляд даних:** http://localhost:8080/viewAll
- **REST API:** http://localhost:8080/api

---

## 👥 Облікові записи

| Користувач | Пароль | Права |
|------------|--------|-------|
| **admin** | admin | Повний доступ (створення, редагування, видалення) |
| **user** | user | Тільки перегляд даних |

---

## 💡 Основні можливості

### Для адміністратора (admin/admin):
- ➕ Додавати країни, періоди, зброю, донорів, поставки
- ✏️ Редагувати всі записи
- 🗑️ Видаляти записи
- 📋 **Створювати запити на допомогу**
- ✅ **Схвалювати запити**
- ❌ **Відхиляти запити**
- 🔍 Шукати за всіма таблицями

### Для користувача (user/user):
- 👀 Переглядати всі дані
- 📊 Бачити таблиці та статистику
- 🚫 Не може редагувати чи видаляти

---

## 🌟 Нова фіча: Система запитів

### Як це працює:

1. **Створення запиту:**
    - Вказуєш тип зброї (Artillery, Air Defense, Vehicles...)
    - Кількість одиниць
    - Пріоритет (URGENT/HIGH/MEDIUM/LOW)
    - Країну-отримувача та донора

2. **Бізнес-правила:**
    - ❌ Зачинені країни не можуть приймати запити
    - ❌ Завершені періоди блокують нові запити
    - ✅ Запити проходять схвалення адміністратором

3. **Статуси запиту:**
   ```
   PENDING   → очікує розгляду
   APPROVED  → схвалено
   REJECTED  → відхилено
   DELIVERED → доставлено
   ```

### Приклад використання:

```
Україна потребує 50 танків Leopard 2
→ Створюємо запит з пріоритетом URGENT
→ Адміністратор переглядає
→ Схвалює або відхиляє
→ Після схвалення створюється поставка
```

---

## 🔌 REST API

### Приклади запитів:

**Отримати всі країни:**
```bash
curl -u admin:admin http://localhost:8080/api/countries
```

**Отримати запити з пріоритетом URGENT:**
```bash
curl -u admin:admin http://localhost:8080/api/requests/priority/URGENT
```

**Схвалити запит:**
```bash
curl -u admin:admin -X PUT http://localhost:8080/api/requests/1/approve
```

**Повний список endpoints:** http://localhost:8080/api

---

## 🛠️ Технології

- **Backend:** Spring Boot 2.7.18, Spring Data JPA
- **База даних:** PostgreSQL 17.2
- **Frontend:** Thymeleaf, HTML/CSS
- **Безпека:** Spring Security (Basic Auth)
- **Контейнеризація:** Docker Compose

---

## 🗄️ База даних

### Підключення через pgAdmin:
1. Відкрий http://localhost:5050
2. Логін: `admin@local.com` / Пароль: `admin`
3. Підключення:
    - Host: `postgres`
    - Port: `5432`
    - Database: `military_aid_db`
    - Username: `postgres`
    - Password: `12345`

---



## 📁 Структура проєкту

```
src/main/java/dev/yurets/db_demo/
├── model/          # Сутності (Country, Period, Weapon, AidRequest...)
├── repository/     # Репозиторії (Spring Data JPA)
├── service/        # Бізнес-логіка
├── controller/     # Web контролери
│   └── rest/       # REST API контролери
└── config/         # Конфігурація (Security)

src/main/resources/
├── templates/      # HTML шаблони (Thymeleaf)
├── static/css/     # Стилі
└── application.properties
```

---

## 🔒 Безпека

- ✅ **SQL Injection:** Захищено через Spring Data JPA
- ✅ **XSS:** Автоматичне екранування в Thymeleaf
- ✅ **Аутентифікація:** Spring Security + BCrypt паролі
- ✅ **Авторизація:** Розмежування доступу за ролями

---

## 🧪 Тестування

Проєкт містить JUnit тести для всіх контролерів:
```bash
./mvnw test
```

**Покриття:**
- CountryController - 10 тестів


---

## 📝 Що нового в останній версії

### ✨ Версія 1.4 - Система запитів на допомогу

- 📋 Повноцінний workflow запитів (PENDING → APPROVED → DELIVERED)
- 🎯 Пріоритизація (URGENT, HIGH, MEDIUM, LOW)
- 🔒 Бізнес-правила (зачинені країни, завершені періоди)
- 🔍 Пошук за всіма параметрами
- 🌐 REST API для інтеграцій
- 👥 Таблиця користувачів (тільки для адміна)
- 🎨 Покращений UI (кнопки стовпцем, не налазять)

---

## 🆘 Проблеми?

### База не підключається?
```bash
docker-compose down
docker-compose up -d
# Зачекайте 10 секунд
./mvnw spring-boot:run
```

### Помилка "table not found"?
```bash
# Застосуйте міграцію
psql -U postgres -d military_aid_db -f aid_requests_migration.sql
```

### Не можу увійти?
- Перевір: `admin` / `admin` (для адміністратора)
- Або: `user` / `user` (для перегляду)

---

## 📧 Автор

**Проєкт виконано для лабораторної роботи**  
Дисципліна: "Технології прикладного програмування"

---

## 📜 Ліцензія

Навчальний проєкт. Використовується для освітніх цілей.

---

**🎉 Готово до використання!**

Запустіть `./mvnw spring-boot:run` та відкрийте http://localhost:8080
